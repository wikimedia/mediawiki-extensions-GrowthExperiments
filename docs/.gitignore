# Ignore generated files from the VitePress project in documentation/frontend
# .gitignore can't be placed inside docs/frontend because VitePress build
# empties the directory when building.
frontend/*
